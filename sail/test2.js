// Given an array of integers nums and an integer target, return
// indices of the two numbers such that they add up to target.

// Input: nums = [2,7,11,15], target = 9Output: [0,1]Explanation: Because nums[0] + nums[1] == 9, we return [0, 1].

//always an array of numbers
//output is
//nested for loop to check the first number
//second for loop would check to see if futures sum/added to 9
//if it does then sum
//if it doesn't it iterates the next number in the array
//and it continues to end of array
//return not possible
function addup9(array) {
  for (i = 0; i < array.length; i++) {}
  return;
}
