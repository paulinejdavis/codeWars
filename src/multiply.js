const multiply = (a, b) => (a * b)

// const multiply: declares a constant variable named multiply.
// = (a, b) =>: assigns an arrow function with two parameters a and b to the multiply variable.
// (a * b): returns the result of multiplying the two parameters a and b.


// function multiply(a, b){
//   return (a * b)
// }

// function multiply(a, b): declares a function named multiply with two parameters a and b.
// return (a * b): returns the result of multiplying the two parameters a and b.