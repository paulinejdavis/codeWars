var sortArray = function(value) {
    return value.split('').sort().join('');
  }
  
  // const sortArray = value =>
  //   [...value].sort().join(``);
  
  
  // var sortArray = function(value) {
  //     return value.split('').sort(function(c, p){return  c - p }).join('');
  // }
  
  //   return value.split('').sort(function(c, p) => c / p ).join('');
  
  